import React from "react";

function PrimaryButton(props) {
  return <button className={`${props.className} rounded-5px py-4 px-3 bg-primary6 flex-center gap-3 text-white text-sm`}>
    
  </button>;
}

export default PrimaryButton;
